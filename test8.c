#include <stdio.h>
int main()
{
    // char c;
    // printf("enter the first symbol: ");
 
    // // 读取用户输入
    // scanf("%c", &c);  
    
    // // %d 显示整数
    // // %c 显示对应字符
    // printf("%c's correspondant ASCII is %d", c, c);
    // return 0;

    /*test*/
    char symbol[]="da";
    int symbol1;
    // symbol = 'da';
    symbol1 = 122.0000;

    // printf("# is %s\n",symbol);
    printf("# is %s\n",symbol);
    printf("#1 is %d\n",symbol1);
    printf("#1 is %x\n",symbol1);
    printf("#1 is %c\n",symbol1);
}